<script>
  (function () {
    const bar = document.getElementById("barFill") || document.querySelector(".bar-fill");
    if (!bar) return;

    bar.style.width = "0%";

    let p = 0;
    const cap = 92;
    const timer = setInterval(() => {
      if (p < cap) {
        p += (Math.random() * 1.2) + 0.4;
        if (p > cap) p = cap;
        bar.style.width = Math.floor(p) + "%";
      }
    }, 220);

    function finishTo100() {
      clearInterval(timer);
      let current = Math.floor(p);
      const fin = setInterval(() => {
        current += 2;
        if (current >= 100) {
          current = 100;
          clearInterval(fin);
        }
        bar.style.width = current + "%";
      }, 30);
    }

    if (window.Blazor && typeof window.Blazor.start === "function") {
      window.Blazor.start().then(() => {
        finishTo100();
      });
    } else {
      document.addEventListener("DOMContentLoaded", () => {
        const wait = setInterval(() => {
          if (window.Blazor && typeof window.Blazor.start === "function") {
            clearInterval(wait);
            window.Blazor.start().then(() => {
              finishTo100();
            });
          }
        }, 50);
      });
    }
  })();
</script>


.top-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
    margin: 0 20px;
    padding: 8px 0;
}

.contenedor-boton {
    min-width: 150px;
    border: 1px solid #e2e2e2;
    padding: 6px 10px;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 9px;
    border-radius: 8px;
    cursor: pointer;
}

.modalOption {
    position: absolute;
    width: 400px;
    background-color: white;
    z-index: 10;
    top: 25px;
    border-radius: 11px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

.container-scrollable {
    height: calc(100vh - 10vh);
    overflow-y: auto;
}.toolbar-container {
    display: flex;
    justify-content: space-between;
    margin: 0 20px;
    padding: 12px;
    background: linear-gradient(135deg, #ff6a6a, #ff3d3d);
    border-radius: 14px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.15);
}

/* Tarjetas */
.contenedor-boton {
    background: white;
    border-radius: 12px;
    padding: 10px 14px;
    box-shadow: 0 4px 10px rgba(0,0,0,0.12);
    transition: all 0.2s ease;
    cursor: pointer;
}

.contenedor-boton:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 16px rgba(0,0,0,0.18);
}


@*Barra de Orden/Filtro*@
<div style="width:12%; position:relative; @(FilterList.Count > 0 ? "" : "display:none")">

    @*Boton de Orden*@
    <div @onclick="showModal"
         class="orderFilterBtn"
         style="@(FilterList.Count == 0 ? "display:none" : "display:flex")">

        <Icons Name="arrow-down-up" Width="16" Height="16" />

        @if (FilterList.Count > 1)
        {
            <span style="font-size:small">@FilterList.Count órdenes</span>
        }
        else
        {
            <span style="font-size:small">@FirstFilter</span>
        }
    </div>

</div>

/* ===========================
   BOTÓN ORDEN/FILTRO
   =========================== */
.orderFilterBtn{
  display:flex;
  gap:8px;
  align-items:center;
  justify-content:center;

  width:100%;
  height:26px;

  border-radius:30px;
  border:1px solid rgba(255,255,255,.55);

  background: transparent;        /* NO azul */
  color:#fff !important;          /* TEXTO BLANCO */
}

/* Forzar blanco en texto dentro */
.orderFilterBtn :deep(*){
  color:#fff !important;
}

/* Forzar blanco en iconos SVG */
.orderFilterBtn :deep(svg),
.orderFilterBtn :deep(svg *){
  fill:#fff !important;
  stroke:#fff !important;
}


/* ==========================================================
   TOPBAR (Barra roja: Tabs + Filtros + Buscador + Opciones)
   ========================================================== */

.topbar{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:14px;

  /* Barra fina */
  height:44px;
  padding:0 16px;
  margin:10px 12px 0 12px;

  /* Davivienda vibes */
  background: linear-gradient(90deg, #B31217 0%, #D21F26 45%, #B31217 100%);
  border-radius:14px;

  box-shadow: 0 10px 22px rgba(0,0,0,.18);
}

/* Lados */
.topbar-left,
.topbar-right{
  display:flex;
  align-items:center;
  gap:10px;
}

/* ==========================================================
   BOTONES / OPCIONES (Todos, Tablero, Cronograma, iconos)
   Nota: <Option ... HandleClass="SeleccionarOpcion" />
   ========================================================== */

.SeleccionarOpcion{
  display:flex;
  align-items:center;
  gap:8px;

  padding:8px 12px;
  height:32px;

  border-radius:12px;
  cursor:pointer;

  /* transparente pero visible */
  background: rgba(255,255,255,.10);
  border: 1px solid rgba(255,255,255,.22);

  /* letras blancas */
  color:#fff !important;

  transition: transform .08s ease, background .15s ease, border-color .15s ease;
}

/* Hover */
.SeleccionarOpcion:hover{
  background: rgba(255,255,255,.16);
  border-color: rgba(255,255,255,.28);
  transform: translateY(-1px);
}

/* Activo (por si tu componente marca active/activo) */
.SeleccionarOpcion.active,
.SeleccionarOpcion.activo,
.SeleccionarOpcion.is-active{
  background: rgba(255,255,255,.22);
  border-color: rgba(255,255,255,.38);
}

/* Forzar texto blanco dentro del Option */
.SeleccionarOpcion :deep(*){
  color:#fff !important;
}

/* Forzar iconos (svg) en blanco */
.SeleccionarOpcion :deep(svg),
.SeleccionarOpcion :deep(svg *){
  fill:#fff !important;
  stroke:#fff !important;
}

/* ==========================================================
   BUSCADOR (blanco, redondeado, placeholder en negrita)
   ========================================================== */

.topbar-search{
  display:flex;
  align-items:center;
}

.topbar-search-input{
  width:260px;
  height:32px;

  padding:0 14px;
  border-radius:999px;

  background:#ffffff;
  border:1px solid rgba(255,255,255,.45);
  outline:none;

  font-weight:700;           /* “Buscar…” y texto en negrita */
  color:#2b2b2b;
  box-shadow: inset 0 2px 6px rgba(0,0,0,.08);
}

.topbar-search-input::placeholder{
  font-weight:700;
  color:#5a5a5a;
}

/* ==========================================================
   ICONOS DE LA DERECHA (filtro, ordenar, buscar, opciones)
   Si alguno no está usando SeleccionarOpcion, igual lo forzamos
   ========================================================== */

.topbar-right :deep(svg),
.topbar-right :deep(svg *),
.topbar-left  :deep(svg),
.topbar-left  :deep(svg *){
  fill:#fff !important;
  stroke:#fff !important;
}

/* ==========================================================
   BOTÓN ORDEN/FILTRO (el que ya estabas tocando)
   ========================================================== */

.orderFilterBtn{
  display:flex;
  gap:8px;
  align-items:center;
  justify-content:center;

  width:100%;
  height:26px;

  border-radius:30px;
  border:1px solid rgba(255,255,255,.55);

  background: transparent;
  color:#fff !important;
}

.orderFilterBtn :deep(*){
  color:#fff !important;
}

.orderFilterBtn :deep(svg),
.orderFilterBtn :deep(svg *){
  fill:#fff !important;
  stroke:#fff !important;
}